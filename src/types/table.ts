export interface TableEntry {
  id: string;
  date: string;
  time: string;
  preBox: string;
  boxInside: string;
  boxOutside: string;
  driver: string;
  plate: string;
  destination: string;
  observation: string;
} 